<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd





   	http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context.xsd

   	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
   	http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
   	http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd">
    <aop:aspectj-autoproxy proxy-target-class="true" />
    <context:annotation-config />
    <context:component-scan base-package="com.meiliwan.emall.cms.*" />

    <bean id="zkResource" class="com.meiliwan.emall.commons.plugin.ZookeeperResource">
	 	<property name="zkNodePaths">
            <value>cms-service/jdbc.properties</value>
        </property>
    </bean>
    <!-- 加载资源配置信息   -->
    <bean id="propertyConfig"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="order" value="2" />
		<property name="ignoreUnresolvablePlaceholders" value="true" />
		<property name="location">
			<ref bean="zkResource" />
		</property>
        <property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
    </bean>
    
    <bean id="mainDataSource" class="com.jolbox.bonecp.BoneCPDataSource" lazy-init="true">
        <property name="driverClass" value="com.mysql.jdbc.Driver"></property>
        <property name="jdbcUrl" value="${cms.jdbc.url.1}"></property>
        <property name="username" value="${cms.jdbc.username.1}"></property>
        <property name="password" value="${cms.jdbc.password.1}"></property>
        <property name="defaultAutoCommit" value="${cms.jdbc.defaultAutoCommit.1}"></property>
        <!-- 每个分区最大的连接数 -->
        <property name="maxConnectionsPerPartition" value="10"/>
        <!-- 每个分区最小的连接数 -->
        <property name="minConnectionsPerPartition" value="5"/>
        <!-- 分区数 ，默认值2，最小1，推荐3-4，视应用而定-->
        <property name="partitionCount" value="3"/>
        <!-- 每次去拿数据库连接的时候一次性要拿几个,默认值：2 -->
        <property name="acquireIncrement" value="${cms.jdbc.acquireIncrement.1}"/>
        <!-- 缓存prepared statements的大小，默认值：0 -->
        <property name="statementsCacheSize" value="${cms.jdbc.statementsCacheSize.1}"/>
        <!-- 每个分区释放链接助理进程的数量，默认值：3，除非你的一个数据库连接的时间内做了很多工作，不然过多的助理进程会影响你的性能 -->
        <property name="releaseHelperThreads" value="3"/>
        <property name="idleConnectionTestPeriodInMinutes" value="${cms.jdbc.idleConnectionTestPeriodInMinutes.1}" />
        <property name="connectionTestStatement" value="${cms.jdbc.connectionTestStatement.1}"  />
        <property name="idleMaxAgeInMinutes" value="${cms.jdbc.idleMaxAgeInMinutes.1}"/>
    </bean>


    <bean id="dataSourceSlave0" class="com.jolbox.bonecp.BoneCPDataSource" lazy-init="true">
        <property name="driverClass" value="com.mysql.jdbc.Driver"></property>
        <property name="jdbcUrl" value="${cms.jdbc.url.2}"></property>
        <property name="username" value="${cms.jdbc.username.2}"></property>
        <property name="password" value="${cms.jdbc.password.2}"></property>
        <property name="defaultAutoCommit" value="${cms.jdbc.defaultAutoCommit.2}"></property>
        <!-- 每个分区最大的连接数 -->
        <property name="maxConnectionsPerPartition" value="${cms.jdbc.maxConnectionsPerPartition.2}"/>
        <!-- 每个分区最小的连接数 -->
        <property name="minConnectionsPerPartition" value="${cms.jdbc.minConnectionsPerPartition.2}"/>
        <!-- 分区数 ，默认值2，最小1，推荐3-4，视应用而定-->
        <property name="partitionCount" value="${cms.jdbc.partitionCount.2}"/>
        <!-- 每次去拿数据库连接的时候一次性要拿几个,默认值：2 -->
        <property name="acquireIncrement" value="${cms.jdbc.acquireIncrement.2}"/>
        <!-- 缓存prepared statements的大小，默认值：0 -->
        <property name="statementsCacheSize" value="${cms.jdbc.statementsCacheSize.2}"/>
        <!-- 每个分区释放链接助理进程的数量，默认值：3，除非你的一个数据库连接的时间内做了很多工作，不然过多的助理进程会影响你的性能 -->
        <property name="releaseHelperThreads" value="${cms.jdbc.releaseHelperThreads.2}"/>
        <property name="idleConnectionTestPeriodInMinutes" value="${cms.jdbc.idleConnectionTestPeriodInMinutes.2}" />
        <property name="connectionTestStatement" value="${cms.jdbc.connectionTestStatement.2}"  />
        <property name="idleMaxAgeInMinutes" value="${cms.jdbc.idleMaxAgeInMinutes.2}"/>
    </bean>
    <bean id="dataSourceSlave1" class="com.jolbox.bonecp.BoneCPDataSource" lazy-init="true">
        <property name="driverClass" value="com.mysql.jdbc.Driver"></property>
        <property name="jdbcUrl" value="${cms.jdbc.url.3}"></property>
        <property name="username" value="${cms.jdbc.username.3}"></property>
        <property name="password" value="${cms.jdbc.password.3}"></property>
        <property name="defaultAutoCommit" value="${cms.jdbc.defaultAutoCommit.3}"></property>
        <!-- 每个分区最大的连接数 -->
        <property name="maxConnectionsPerPartition" value="${cms.jdbc.maxConnectionsPerPartition.3}"/>
        <!-- 每个分区最小的连接数 -->
        <property name="minConnectionsPerPartition" value="${cms.jdbc.minConnectionsPerPartition.3}"/>
        <!-- 分区数 ，默认值2，最小1，推荐3-4，视应用而定-->
        <property name="partitionCount" value="${cms.jdbc.partitionCount.3}"/>
        <!-- 每次去拿数据库连接的时候一次性要拿几个,默认值：2 -->
        <property name="acquireIncrement" value="${cms.jdbc.acquireIncrement.3}"/>
        <!-- 缓存prepared statements的大小，默认值：0 -->
        <property name="statementsCacheSize" value="${cms.jdbc.statementsCacheSize.3}"/>
        <!-- 每个分区释放链接助理进程的数量，默认值：3，除非你的一个数据库连接的时间内做了很多工作，不然过多的助理进程会影响你的性能 -->
        <property name="releaseHelperThreads" value="${cms.jdbc.releaseHelperThreads.3}"/>
        <property name="idleConnectionTestPeriodInMinutes" value="${cms.jdbc.idleConnectionTestPeriodInMinutes.3}" />
        <property name="connectionTestStatement" value="${cms.jdbc.connectionTestStatement.3}"  />
        <property name="idleMaxAgeInMinutes" value="${cms.jdbc.idleMaxAgeInMinutes.3}"/>
    </bean>
    <bean id="dataSourceSlave2" class="com.jolbox.bonecp.BoneCPDataSource" lazy-init="true">
        <property name="driverClass" value="com.mysql.jdbc.Driver"></property>
        <property name="jdbcUrl" value="${cms.jdbc.url.4}"></property>
        <property name="username" value="${cms.jdbc.username.4}"></property>
        <property name="password" value="${cms.jdbc.password.4}"></property>
        <property name="defaultAutoCommit" value="${cms.jdbc.defaultAutoCommit.4}"></property>
        <!-- 每个分区最大的连接数 -->
        <property name="maxConnectionsPerPartition" value="${cms.jdbc.maxConnectionsPerPartition.4}"/>
        <!-- 每个分区最小的连接数 -->
        <property name="minConnectionsPerPartition" value="${cms.jdbc.minConnectionsPerPartition.4}"/>
        <!-- 分区数 ，默认值2，最小1，推荐3-4，视应用而定-->
        <property name="partitionCount" value="${cms.jdbc.partitionCount.4}"/>
        <!-- 每次去拿数据库连接的时候一次性要拿几个,默认值：2 -->
        <property name="acquireIncrement" value="${cms.jdbc.acquireIncrement.4}"/>
        <!-- 缓存prepared statements的大小，默认值：0 -->
        <property name="statementsCacheSize" value="${cms.jdbc.statementsCacheSize.4}"/>
        <!-- 每个分区释放链接助理进程的数量，默认值：3，除非你的一个数据库连接的时间内做了很多工作，不然过多的助理进程会影响你的性能 -->
        <property name="releaseHelperThreads" value="${cms.jdbc.releaseHelperThreads.4}"/>
        <property name="idleConnectionTestPeriodInMinutes" value="${cms.jdbc.idleConnectionTestPeriodInMinutes.4}" />
        <property name="connectionTestStatement" value="${cms.jdbc.connectionTestStatement.4}"  />
        <property name="idleMaxAgeInMinutes" value="${cms.jdbc.idleMaxAgeInMinutes.4}"/>
    </bean>



    <bean id="cmsSqlSessionFactory"
          class="com.meiliwan.emall.core.db.shard.spring.support.SqlSessionFactoryBean">
        <property name="mainDataSource" ref="mainDataSource" />
        <property name="shardDataSourceList">
            <util:list>
                <ref bean="dataSourceSlave0" />
                <ref bean="dataSourceSlave1" />
                <ref bean="dataSourceSlave2" />
            </util:list>
        </property>
        <property name="mapperLocations">
            <array>
                <value>classpath:conf/mapper/cms/*.xml</value>
            </array>
        </property>
        <property name="shardStrategy">
            <map>
                <entry key="CommShard">
                    <value>com.meiliwan.emall.core.db.shard.strategy.CommShardStrategy</value>
                </entry>
            </map>
        </property>
    </bean>

    <!-- 事务配置 -->
    <bean name="cmsTransactionManager"
          class="com.meiliwan.emall.core.db.shard.spring.support.MultiDataSourceTransactionManager">
    </bean>
    <tx:advice id="cmsTxAdvice" transaction-manager="cmsTransactionManager">
    <tx:attributes>
           <tx:method name="*" propagation="NOT_SUPPORTED"/>
        </tx:attributes>
    </tx:advice>

    <aop:config>
        <!-- UserService事务管理 -->
        <aop:pointcut id="cmsServiceOperation"
                      expression="execution(* com.meiliwan.emall.*.dao.impl.*.*(..)) || execution(* com.meiliwan.emall.core.db.BaseDao.*(..))" />

        <aop:advisor advice-ref="cmsTxAdvice" pointcut-ref="cmsServiceOperation" />
    </aop:config>

    <bean id="performanceMonitor"
          class="com.meiliwan.emall.core.interceptor.DaoPerfMonitorInterceptor" />
    <aop:config>
        <aop:pointcut id="allDaoMethods" expression="execution(* com.meiliwan.emall.*.dao.impl.*.*(..)) || execution(* com.meiliwan.emall.core.db.BaseDao.*(..))"/>
        <aop:advisor pointcut-ref="allDaoMethods" advice-ref="performanceMonitor"  />
    </aop:config>

</beans>
